# v0.10.14 copyright-safe UI final

This SourceKit is based on the v0.10.12 legacy route.

Changes:
- Removed the overly broad v0.10.13 wiki-name bulk import approach.
- Kept only QoL 1.0.4.5 UI labels and targeted internal dropdown IDs from QoL source lists.
- Added `Seed` -> `种子` and `Custom settings` -> `自定义设置`.
- Exact translations count: 944.

Build:
```powershell
cd "D:\ModWork\QoLChinesePatch_SourceKit_v0.10.14_copyright_safe_ui_final"
.\build.ps1 -GameDir "D:\steam\steamapps\common\Casualties Unknown Demo"
```
