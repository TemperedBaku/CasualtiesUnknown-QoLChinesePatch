# 稳定版蓝图：QoL Unknown Chinese Patch v0.10.9

## 必要链路

1. Bootstrap 早加载，先加载词表。
2. Patch Text/TMP setter 和 IMGUI 文本入口。
3. 创建隐藏 disabled-button trigger，补齐 QoL 某些生命周期条件。
4. 等待 QoL 自己创建 EnhancedSettingsButton。
5. 发现现有 QoL 按钮时，不追加 onClick；只 arm auto-prime。
6. auto-prime 隐藏式调用 EnhancedMenuController.OpenMenu()，让 EnhancedSettingsPanel 实例化。
7. 对菜单对象做短 burst 翻译。
8. 菜单恢复 inactive。
9. 后续通过 QoL source-based hooks 和 click-trigger burst scan 处理 tab/绑定页/重建 UI。

## 可砍/默认关闭

- 旧 KrokMP-style lifecycle shim：0.10.6 以后不再是成功核心，默认关闭。
- SourceEnhancedButtonObjectWatcher：Bootstrap watcher 已覆盖主需求。
- PreciseEnhancedSettingsButtonHook：容易和按钮点击逻辑重叠，默认关闭。
- Missing dump/trace：只留给调试版。
- ExistingSceneScan/SceneLoadedScan/ESC watcher/AutoScanWatcher：不作为发布版默认路径。

## 不要砍

- Bootstrap Text/TMP setter patch。
- Bootstrap GUI/IMGUI patch。
- Hidden disabled-button trigger。
- EnhancedSettingsButton rescue watcher + auto-prime。
- Source-based EnhancedMenu hooks。
- EnhancedSettingsPanel localizer。
- Click-trigger burst scan：暂时保留，用于点击 tab 后 UI 重建兜底。
